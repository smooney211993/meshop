import {
  ORDER_PAY_REQUEST,
  ORDER_PAY_SUCCESS,
  ORDER_PAY_FAIL,
  ORDER_PAY_RESET,
} from '../actions/types';

const initialState = {};

export default function (state = initialState, action) {
  return state;
}
