import {
  ORDER_CREATE_FAIL,
  ORDER_CREATE_REQUEST,
  ORDER_CREATE_SUCCESS,
} from '../actions/types';

const initialState = {};

export default function (state = initialState, action) {
  return state;
}
